[project]
name = "currency-converter"
version = "0.1.0"
description = "Currency conversion microservice (FastAPI + gRPC, ECB rate feed)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
    "grpcio>=1.66.0",
    "grpcio-health-checking>=1.66.0",
    "grpcio-status>=1.66.0",
    "googleapis-common-protos>=1.65.0",
    "protobuf>=5.28.0",
    "httpx>=0.27.2",
    "python-json-logger>=2.0.7",
]

[dependency-groups]
dev = [
    "pytest>=8.3.3",
    "pytest-asyncio>=0.24.0",
    "ruff>=0.7.0",
    "respx>=0.21.1",
    "grpcio-tools>=1.66.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "N", "SIM", "RUF"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
